<svg xmlns="http://www.w3.org/2000/svg" width="520" height="220" viewBox="0 0 520 220" role="img"
     aria-label="Dark-mode theme control before and after Issue 178: the grey square behind the circular button is gone.">
  <title>Issue #178 — dark-mode theme control, before and after</title>
  <desc>Derived illustration, not a browser screenshot: every colour below is the
  value tests/theme-toggle-shape-check.js computes from the real cascade of
  docs/styles.css + docs/theme.css over the dark page background.</desc>

  <!-- Dark page background: [data-theme="dark"] --page-bg first stop, #1a1a2e -->
  <rect width="520" height="220" fill="#1a1a2e"/>

  <text x="30" y="34" font-family="system-ui, sans-serif" font-size="14" fill="#e8e8f2">
    Before — container scrim paints a square
  </text>
  <!-- Container: rgba(255,255,255,0.12) over #1a1a2e, border-radius: none -->
  <rect x="30" y="52" width="38" height="38" fill="#353547"/>
  <!-- Button: rgba(255,255,255,0.16) over that container -->
  <circle cx="49" cy="71" r="19" fill="#555564"/>
  <text x="49" y="77" font-size="17" text-anchor="middle">&#x1F319;</text>
  <text x="30" y="120" font-family="system-ui, sans-serif" font-size="12" fill="#9aa0b8">
    rgb(53, 53, 71) square, 27.5 per-channel off the page
  </text>

  <text x="290" y="34" font-family="system-ui, sans-serif" font-size="14" fill="#e8e8f2">
    After — only the circular button paints
  </text>
  <!-- Container transparent; button rgba(255,255,255,0.16) straight over the page -->
  <circle cx="309" cy="71" r="19" fill="#3f3f4f"/>
  <text x="309" y="77" font-size="17" text-anchor="middle">&#x1F319;</text>
  <text x="290" y="120" font-family="system-ui, sans-serif" font-size="12" fill="#9aa0b8">
    container paints nothing; button stays 36.6 off the page
  </text>

  <text x="30" y="170" font-family="system-ui, sans-serif" font-size="12" fill="#9aa0b8">
    Derived from the computed cascade by tests/theme-toggle-shape-check.js —
  </text>
  <text x="30" y="188" font-family="system-ui, sans-serif" font-size="12" fill="#9aa0b8">
    this is an illustration of those values, not a browser screenshot (no
  </text>
  <text x="30" y="206" font-family="system-ui, sans-serif" font-size="12" fill="#9aa0b8">
    browser or Playwright MCP is available on this build host).
  </text>
</svg>
